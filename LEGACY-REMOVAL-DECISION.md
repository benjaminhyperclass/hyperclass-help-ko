# 레거시 `dashboard-ko.min.js@8fabb6a` 제거 판단

**작성** 2026-08-23 · **권고** 지금 제거하지 말 것. **ALLOW 개방 → 관찰 → 그다음 제거.**

## 실측

레거시 사전을 `@8fabb6a` 소스에서 추출해 v4 와 대조했습니다 (10,300건).

| | 건수 | 비율 |
|---|---:|---:|
| v4 가 커버 (`_text` 9,088 + i18n 원문 47,272) | 10,286 | **99.7%** |
| v4 미커버 = 제거 시 실손실 | 14 | 0.3% |
| ㄴ 이번에 `_text` 로 회수 (v4.2.3) | 10 | |
| ㄴ 격리 — 테마 고유명 3 + `N/A` 단문 1 | 4 | |
| 값 충돌 (같은 영문, 다른 한국어) | 10 / 1,440 공통 | 0.7% |

**회수를 마쳤으므로 레거시 제거의 실손실은 이제 사실상 0 입니다.**

충돌 10건은 전부 문구 차이라 기능 영향이 없습니다
(`Go Back`: 돌아가기/뒤로 가기, `Load More`: 더 불러오기/더 보기 …).

## 순서가 중요합니다 — 지금 제거하면 사고입니다

v4 는 `ALLOW = ['r6JD1nsqtk6Oln28fgrj']` 로 **한 곳에만** 걸려 있습니다.
지금 레거시를 지우면 **나머지 23개 서브계정과 에이전시 화면 전체가 영어로 돌아갑니다.**
레거시가 그 계정들의 유일한 한국어 레이어입니다.

## 반대로, ALLOW 개방을 서둘러야 할 이유가 있습니다

레거시 사전의 한국어 값 **55건에 화이트라벨 안 된 브랜드가 박혀 있습니다.**

```
'HighLevel 지원팀에 전화하기'
'+1(888)732-4197로 HighLevel에 전화하세요'      ← HighLevel 지원 전화번호
'공식 HighLeveler가 되신 것을 축하드립니다!'
'확인하면 LeadConnector가 이 결제에 대해 카드를 청구할 수 있습니다.'
```

v4 사전은 C1 검사로 브랜드 잔존 0 이 강제되지만, 레거시는 그 검사를 받지 않습니다.

**허용 로케이션에서는 55건 전부 잠들어 있습니다** — v4 가 i18n 레이어에서 먼저
한국어로 렌더하므로 레거시의 영문 완전일치가 발동하지 않습니다 (54건 i18n / 1건 `_text`).
**그러나 게이트 밖 23개 계정에서는 지금 이 순간 그대로 노출 중입니다.**

즉 ALLOW 개방은 커버리지 확대인 동시에 **브랜드 누출 차단**입니다.

## 덧붙임 — 설치된 레거시가 2.5개월 낡았습니다

| | 날짜 | 크기 |
|---|---|---:|
| 설치본 `@8fabb6a` | 2026-06-10 | 660 KB |
| 저장소 최신 `09ab989` | 2026-08-21 | 2,719 KB |

저장소에 4배 큰 최신 빌드가 있는데 배포되지 않았습니다. 다만 v4 로 대체할 계획이므로
**레거시를 갱신하지 말고 그대로 두었다가 제거하는 편**이 낫습니다 — 갱신하면 검사받지 않은
문자열이 더 늘어납니다.

## 권고 순서

1. ~~레거시 전용 10건 회수~~ ✅ v4.2.3 배포 완료 — 제거 시 손실 제거
2. **ALLOW 개방** (레거시는 유지). 두 레이어 공존 상태로 관찰
   - 관찰 지표: `textHits` 증가 · `unmatched` · `fallback` 0 · `fuzzy` 0 · `remerge`
   - 예상 충돌면: 거의 0. v4 가 렌더 시점에 이미 한국어로 만들어 레거시가 발동하지 않음
3. 서브계정 24개 단문 전수 스캔 + 스냅샷·템플릿 키 검증 (에이전시 레이어라 이 시점에만 가능)
4. 안정 확인 후 **레거시 3번째 `<script>` 줄 제거**

3번에서 격리 중인 4건(`Azure Dreams` `Midnight Deep` `Royal Velvet` `N/A`)도 함께 판정합니다.

## 하지 말 것

- 2번을 건너뛰고 4번으로 가기 — 23개 계정이 영어가 됩니다
- 레거시를 최신 빌드로 갱신하기 — 검사받지 않은 문자열만 늘어납니다
