# 용어 분기 백로그

**기록** 2026-08-22 · 레거시 회수 머지 중 발견 · **이번 라운드에서는 손대지 않음**

회수 대상을 대조하다 배포 사전 안에 같은 개념이 두 용어로 갈려 있는 것이 드러났습니다.
번역 품질 문제가 아니라 **여러 번역 라운드가 서로 다른 용어를 택한 결과**입니다.
한 번에 정리하면 diff가 수천 줄이 되므로 별도 라운드로 분리합니다.

## 실측 (2026-08-22 배포 사전 `host` + `_text`)

| 개념 | A | B | 비고 |
|---|---:|---:|---|
| 청구/결제 | `청구` 219 | `결제` 490 | `billing` vs `payment` 원문 구분이 섞임 |
| 서브계정/위치 | `서브 계정` 324 | `위치` 417 | 원문은 둘 다 `location`(=GHL 서브계정) |
| 제거/삭제 | `제거` 357 | `삭제` 1,111 | `remove` vs `delete` 구분 필요 |
| 로케이션/위치 | `로케이션` 21 | `위치` 417 | 음차와 번역 혼재 |
| 클라이언트/고객 | `클라이언트` 171 | `고객` 531 | 원문은 `client` vs `customer` |

## 판단이 필요한 지점

**`location`이 제일 중요합니다.** GHL에서 `location`은 지리적 위치가 아니라 **서브계정**입니다.
`위치` 417건 중 상당수가 오역일 수 있는데, 일부는 실제 주소·지역을 뜻해 일괄 치환이 불가능합니다.
원문 대조 없이는 못 가릅니다.

`billing`/`payment`, `remove`/`delete`, `client`/`customer`는 **원문에서 구분되는 별개 단어**입니다.
한쪽으로 통일하는 게 아니라 원문에 맞춰 매핑을 고정하는 것이 맞습니다.

## 권장 순서

1. `_source/hc-ko-app-reference.json`(원문 대조본)으로 개념별 원문 분포를 먼저 뽑습니다
2. 원문↔번역 매핑을 확정해 글로서리로 등재합니다
3. 위반분만 골라 고칩니다 — 전량 치환은 하지 않습니다
4. 검사기에 글로서리 규칙을 추가해 재발을 막습니다

## 하지 말 것

- **일괄 치환 금지.** `위치`→`서브 계정` 같은 전량 치환은 실제 주소 문구를 깨뜨립니다
- 이 정리와 사전 회수를 같은 커밋에 섞지 말 것 — 회수분 검증이 묻힙니다
